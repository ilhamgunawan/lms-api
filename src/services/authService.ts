const createLogin = () => {
  return;
};

const deleteLogin = () => {
  return;
};

const getSessionStatus = () => {
  return;
};

const getCurrentRole = () => {
  return;
};

const authService = {
  createLogin,
  deleteLogin,
  getSessionStatus,
  getCurrentRole,
};

export default authService;
